<?xml version="1.0"?>
<psalm
    errorLevel="4"
    findUnusedCode="false"
    findUnusedBaselineEntry="true"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
    errorBaseline="tests/psalm-baseline.xml"
>
	<stubs>
		<file name="tests/stub.phpstub" preloadClasses="true"/>
	</stubs>
	<projectFiles>
		<directory name="lib" />
		<ignoreFiles>
			<directory name="vendor" />
		</ignoreFiles>
	</projectFiles>
	<extraFiles>
		<directory name="vendor" />
		<ignoreFiles>
			<directory name="vendor/phpunit/php-code-coverage" />
		</ignoreFiles>
	</extraFiles>
	<issueHandlers>
		<!--
			Psalm believes every property of a SimpleXMLElement is set, so it
			reads the `?? null` guards in the feed parser as dead code. They
			are not: one level deep a missing child is an empty element, two
			levels deep it is null, and `$item->foo->bar` on a feed that omits
			`foo` is a method call on null. The guards stay and the two rules
			that cannot see that are off for the one file that reads feeds
			somebody else wrote.
		-->
		<TypeDoesNotContainType>
			<errorLevel type="suppress">
				<file name="lib/Service/FeedParserService.php"/>
			</errorLevel>
		</TypeDoesNotContainType>
		<RedundantCondition>
			<errorLevel type="suppress">
				<file name="lib/Service/FeedParserService.php"/>
			</errorLevel>
		</RedundantCondition>
		<TypeDoesNotContainNull>
			<errorLevel type="suppress">
				<file name="lib/Service/StreamService.php"/>
			</errorLevel>
		</TypeDoesNotContainNull>
	</issueHandlers>
</psalm>
