# SPDX-FileCopyrightText: 2018 Nextcloud GmbH and Nextcloud contributors
# SPDX-License-Identifier: AGPL-3.0-or-later
\.idea/
node_modules/
build/
vendor/
img/twemoji/

cypress/screenshots
cypress/snapshots

.php-cs-fixer.cache
.vscode/settings.json

js/*.map

# NEVER commit these - private key and certificate signing request
*.key
*.csr
*.crt
RELEASE.md

# what the translation extractor leaves behind when it is run locally
# (.github/workflows/l10n.yml): the .pot it writes, the tool itself, and the
# file it concatenates every Vue template into for xgettext to read
translationfiles/
translationtool.phar
specialVueFakeDummyForL10nScript.js

# test artefacts
tests/.phpunit.result.cache
coverage/
# Playwright
playwright-report/
test-results/
